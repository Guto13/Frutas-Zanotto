import 'package:flutter/material.dart';

class PaletesScreen extends StatelessWidget {
  const PaletesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}